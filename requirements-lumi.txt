# Generic ROCm dependency superset. The active LUMI workflow keeps these in
# two isolated runtimes (vendor inference container + .venv-aer-lumi); see README.
-r requirements-base.txt
-r requirements-quantum.txt

# torch is supplied by lumi-multitorch; do not replace its ROCm build via pip.
# qiskit-aer-gpu-rocm is installed by the validated source-build script.
# bitsandbytes and llama-cpp-python are intentionally excluded on ROCm.
