{
  "name": "CPI Agent (ROCm)",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "cpi-agent",
  "workspaceFolder": "/workspace",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter", 
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  
  # Auto-install libraries every time you rebuild
  "postCreateCommand": "pip install selenium boto3 webdriver-manager torchvision"
}